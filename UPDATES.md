# 更新记录

> 按时间倒序。未标注版本号，以日期为准。
> 详细技术栈与部署方式见 [README](./README.md)。

## 2026-09-23 · 状态系统收尾 + 播放器细节修复

**状态系统**
- 休息中：状态展示改为**显示主机名**，而非此前直接隐藏，让休息中的设备在状态页依然可见。
- 休息中优先级调低：设备**有其它状态时优先展示其它状态**，只有当设备确实处于休息中才显示。

**播放器细节**
- 修复吸附边界（snap edge）、禁用态样式、标题为空时的保护逻辑。
- 触摸检测：区分移动端与桌面端的交互方式。
- SSE 断线重连增加**退避策略**，避免高频请求。
- 移动端整体布局修正。

**界面样式**
- 文章详情页标题与摘要改用**思源宋体中文字体**（`Source Han Serif SC` → `Noto Serif SC` → `Songti SC` 回退链），与正文等其他部分区分开。

**维护**
- 移除死代码（未使用的 `lastSaved` 字段与 `clearAllStatus`）。

## 2026-09-18 · 代码注释补充 + 版本控制调整

- 为后台共用库、状态页、用户页及各页面补充中文注释，辅助理解代码结构。
- 修复后台共用库的 `escapeHtml` 转义问题。
- 开始**跟踪客户端 Python 源码**（`client/status_client.py`），并忽略 `client/dist/` 构建产物。

## 2026-09-17 · 音频播放体验优化

- 页面**失去焦点时自动暂停音乐**，返回时若用户此前手动暂停过则**尊重手动暂停**，否则恢复播放。
- 播放器优化：自动播放、会话内续播、封面图禁止拖拽。

## 2026-09-09 · 音乐管理功能上线

- 新增**音乐管理**：支持上传、删除音频文件，设置当前播放歌曲。
- 同步更新音乐相关路由与数据库结构，前台播放器自动同步当前歌曲。
- 音频进度条改为**只读**，禁止点击与拖动。
- 修复初始化设置页的跳转目标指向 `/managers/`。
- 客户端不再提交构建产物。

## 2026-08-17 · 安全加固与体验完善

- 登录安全：客户端侧**密码哈希**（服务端常见），修复登录失败与哈希版本迁移问题，移除明文密码传输。
- 新增 `requireAdmin` 中间件、CSP/HSTS 等**安全响应头**、请求输入校验。
- 修复历史安全问题：静态文件暴露、认证绕过、信息泄露。
- 新增美化的 **404 页面**（居中布局、匹配背景与模糊效果）。
- 为所有服务端源码补充中文注释。

## 2026-08-13 · 实时状态显示系统

- 新增**实时状态显示系统**：设备状态通过 SSE 推送，多设备支持，前台/状态页实时展示。
- 首页状态卡片并入 hero 区，更新状态页副标题。
- 状态 API 由 `/api/status` 重命名为 `/api/data`，避免被某些 WAF 规则误拦。

## 2026-08-10 · 初始化与部署完善

- 初始化设置页：首次访问引导创建管理员账号，持久化 `SESSION_SECRET`。
- Docker 部署：`node:24-slim` 基础镜像、强制使用 `gosu` 降权运行修复权限问题、端口 3000 允许外部访问、容器内时区设为 `Asia/Shanghai`（UTC+8）。
- 新增公开搜索页（带限流）。
- README 与项目结构文档补齐。