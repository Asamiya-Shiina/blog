services:
  blog:
    build: .
    container_name: blog
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - blog-data:/app/data

volumes:
  blog-data:
